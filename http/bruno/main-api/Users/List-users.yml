info:
  name: 📋 List users
  type: http
  seq: 1000
http:
  method: GET
  url: '{{mainApi}}/v1/users'
  auth:
    type: bearer
    token: '{{access_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
