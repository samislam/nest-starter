info:
  name: 🧊 Freeze user by ID
  type: http
  seq: 6000
http:
  method: POST
  url: '{{mainApi}}/v1/users/:id/freeze'
  auth:
    type: bearer
    token: '{{access_token}}'
  params:
    - name: id
      value: ''
      type: path
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
