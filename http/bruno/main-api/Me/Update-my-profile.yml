info:
  name: ✏️ Update my profile
  type: http
  seq: 2000
http:
  method: PATCH
  url: '{{mainApi}}/v1/me'
  auth:
    type: bearer
    token: '{{access_token}}'
  body:
    type: json
    data: |-
      {}
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
