info:
  name: 🙋 Get me
  type: http
  seq: 1000
http:
  method: GET
  url: '{{mainApi}}/v1/me'
  auth:
    type: bearer
    token: '{{access_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
