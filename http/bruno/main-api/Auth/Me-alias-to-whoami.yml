info:
  name: 👤 me (alias to whoami)
  type: http
  seq: 4000
http:
  method: GET
  url: '{{mainApi}}/v1/auth/me'
  auth:
    type: bearer
    token: '{{access_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
