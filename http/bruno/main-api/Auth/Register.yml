info:
  name: 📝 Register
  type: http
  seq: 2000
http:
  method: POST
  url: '{{mainApi}}/v1/auth/register'
  body:
    type: json
    data: |-
      {
        "name": "islam",
        "username": "islam",
        "password": "islam"
      }
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
