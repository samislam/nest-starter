info:
  name: 🕵️ whoami
  type: http
  seq: 3000
http:
  method: GET
  url: '{{mainApi}}/v1/auth/whoami'
  auth:
    type: bearer
    token: '{{access_token}}'
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
